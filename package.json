// FILE: package.json
{
  "name": "traveller-ai-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo \"Static build via Vercel\"",
    "start": "vercel dev",
    "lint": "echo \"(add a linter later)\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.46.1",
    "resend": "^3.3.0"
  }
}
